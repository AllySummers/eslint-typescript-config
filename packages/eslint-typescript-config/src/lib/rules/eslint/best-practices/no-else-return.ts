/**
 * @name no-else-return
 * @link https://eslint.org/docs/rules/no-else-return
 * @description disallow  blocks after  statements in  statements
 * @auto-fixable
 */
export interface NoElseReturn {
  /**
   * @default true
   */
  allowElseIf?: boolean;
}
