/**
 * @name valid-typeof
 * @link https://eslint.org/docs/rules/valid-typeof
 * @description enforce comparing  expressions against valid strings
 * @recommended
 */
export interface ValidTypeof {
  /**
   * @default false
   */
  requireStringLiterals?: boolean;
}
