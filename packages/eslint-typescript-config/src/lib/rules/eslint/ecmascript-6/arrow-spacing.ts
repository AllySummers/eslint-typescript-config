/**
 * @name arrow-spacing
 * @link https://eslint.org/docs/rules/arrow-spacing
 * @description enforce consistent spacing before and after the arrow in arrow functions
 * @auto-fixable
 */
export interface ArrowSpacing {
  /**
   * @default true
   */
  before?: boolean;
  /**
   * @default true
   */
  after?: boolean;
}
