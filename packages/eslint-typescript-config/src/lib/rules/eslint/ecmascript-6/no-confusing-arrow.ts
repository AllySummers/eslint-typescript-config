/**
 * @name no-confusing-arrow
 * @link https://eslint.org/docs/rules/no-confusing-arrow
 * @description disallow arrow functions where they could be confused with comparisons
 * @auto-fixable
 */
export interface NoConfusingArrow {
  /**
   * @default true
   */
  allowParens?: boolean;
}
