import type { AlwaysNever } from '../../../util';

/**
 * @name unicode-bom
 * @link https://eslint.org/docs/rules/unicode-bom
 * @description require or disallow Unicode byte order mark (BOM)
 * @auto-fixable
 */
export type UnicodeBom = AlwaysNever;
