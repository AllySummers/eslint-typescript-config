import type { AlwaysNever } from '../../../util';

/**
 * @name new-parens
 * @link https://eslint.org/docs/rules/new-parens
 * @description enforce or disallow parentheses when invoking a constructor with no arguments
 * @auto-fixable
 */
export type NewParens = AlwaysNever;
