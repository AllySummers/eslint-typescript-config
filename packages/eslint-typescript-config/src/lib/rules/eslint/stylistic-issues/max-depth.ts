import type { MaximumOrNumber } from '../../../util';

/**
 * @name max-depth
 * @link https://eslint.org/docs/rules/max-depth
 * @description enforce a maximum depth that blocks can be nested
 */
export type MaxDepth = MaximumOrNumber;