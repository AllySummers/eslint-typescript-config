/**
 * @name semi-spacing
 * @link https://eslint.org/docs/rules/semi-spacing
 * @description enforce consistent spacing before and after semicolons
 * @auto-fixable
 */
export interface SemiSpacing {
  /**
   * @default false
   */
  before?: boolean;
  /**
   * @default true
   */
  after?: boolean;
}
